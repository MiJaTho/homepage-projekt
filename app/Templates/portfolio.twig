{% extends "layout.twig" %}
{% set act = "portfolio" %}
{% block title %}Portfolio{% endblock %}

{% block content %}
      <div id="row">
        {{ csrf_field|raw }}
            
             <h2> Portfolio</h2>
                    
             
             <button class="ui fluid large blue submit button" id="js" name="JavaSpript" value="JavaSpript"> JavaSpript</button>
             <button class="ui fluid large blue submit button" id="php" name="php" value="php"> php</button>
             <button class="ui fluid large blue submit button" id="css" name="css" value="CSS"> CSS</button>
             <div id="out">
             <div id ="code1">
             <h2> jQuery</h2>
            <pre  class="prettyprint">
            
        $("#neu")
        .find("input")
        .on("click", function(e){
            e.preventDefault();    
            var $neuerEintrag = $("#neuerEintrag");
            var $li = $listen.find("li").eq(0).clone();
            $li.contents().eq(4).replaceWith("&nbsp;" + $neuerEintrag.val().trim());
            console.log($li);
            switch(this.value.toLowerCase()) {
                case "oben eintragen" : // Klasse oben ist bereits vorhanden
                $li.appendTo($listen.eq(0))/* .removeClass().addClass("oben") */; 
                break;
                case "unten eintragen":
                $li.appendTo($listen.eq(1)).removeClass().addClass("unten");
                break;

            }


            $neuerEintrag.val("");

        }); // #neu input click  
        </pre>
        </div>
        <div id ="code2">
         <h2> PHP</h2>
 <pre   class="prettyprint"> 

class GalleryController extends Controller
{
    public function __construct(Request $request)
    {
        parent::__construct($request);
        
    }

    public function index()
    {
        
        return $this->render('gallery.twig');
    }

  
}
 </pre>
 </div>
 <div id ="code3">
<h2> CSS</h2> 
 <pre   class="prettyprint">
  
  
  /*navi
+++++++++++++++++++++++++++++++++++++*/
nav {
	clear: both;
	float: left;
	width: 200px;
	padding: 30px;
	background-color: rgba(81,61,7,0.5);
	box-shadow: 0 0 10px #fff inset;
	transition: 1.5s;
}
nav:hover {
	background-color: rgba(81,61,7,0.8);
	box-shadow: 0 0 20px #fff inset;
}
nav ul {
	list-style-type: none;
	border-radius: 10px;
	box-shadow: 0 0 5px #ccc inset;
	padding: 10px;
	background-color: rgba(147,110,16,0.9);
	transition: 1.2s;
}
nav ul:hover {
	box-shadow: 0 0 30px #ccc inset;
	background-color: rgba(147,110,16,0.5);
}
nav ul li a {
	display: block;
	text-decoration: none;
	font-weight: bold;
	background-color: #3b201a;
	color: #fff;
	padding: 10px 5px;
	border-bottom: solid 1px rgba(147,110,16,0.9);
	transition: 1s;
}
nav ul li:nth-last-of-type(1) a {
	border-bottom: none;
}
nav ul li a:hover, .current {
	background-color: #fff;
	color: #3b201a;
	box-shadow: 0 0 20px #fff;
}
.current {
	text-align: center;
} </pre> 
</div>
</div>
           
               
      </div>
{% endblock %}
